services:
  - type: web
    name: ninna-beauty
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: ninna-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
